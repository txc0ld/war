# Lessons

- 2026-03-19: This workspace snapshot has no `.git` directory, so commit-based execution steps must be marked blocked and work tracked in `tasks/todo.md` instead.
- 2026-03-19: When adding Vitest to a Vite 5 workspace, pin a Vite-5-compatible Vitest major instead of taking the latest release line by default.
- 2026-03-19: If demo mode is supposed to bypass a provider stack, do not leave live provider/config initialization at module scope; guard the side effect itself, not just the rendered branch.
- 2026-03-19: A full-screen routed content wrapper above an interactive background will silently steal all map clicks; keep the shell overlay `pointer-events-none` and opt interactive panels back in with `pointer-events-auto`.
- 2026-03-19: TypeScript composite builds can treat a project as up to date if `tsconfig.tsbuildinfo` exists even when `dist/` has been deleted; for publishable workspace packages, force clean-emitting build scripts or validate by deleting build output before deployment.
- 2026-03-20: Battle presentation primitives under `apps/web/src/components/battle` are consumed outside the battle overlay; before tightening prop unions or replacing APIs, inspect every consumer and keep compatibility shims where needed.
- 2026-03-20: For frontend data-only work in this repo, check `apps/web/src/data` before assuming `apps/web/src/lib` is the source of truth; `lib` can contain stale compatibility shims while live registry/config data has moved into `data`.
- 2026-03-20: For interactive SVG maps, stroke-only paths and overlay markers can silently make countries unclickable; give regions a transparent fill hit area and disable pointer events on decorative markers before calling the interaction done.
- 2026-03-20: After deleting and recreating asset-side CSS files during a large frontend rewrite, always rerun workspace build immediately and confirm the file still exists on disk before moving on; import paths can silently drift during concurrent edits.
- 2026-03-20: Before patching frontend CSS during a fast-moving UI pass, read the current selector block from disk first; battle/layout selectors drift often enough that patching from memory wastes cycles and risks missing the live override.
- 2026-03-20: For interactive maps, even subtle container drift animations can make SVG country taps unstable on mobile-sized viewports; keep the frame static and move the emphasis into pulses inside the map instead.
- 2026-03-20: When a responsive fix spans `globals.css` and feature-local CSS, verify which stylesheet loads last before trusting a patch; later feature CSS can silently override global breakpoint rules and make viewport fixes appear ineffective.
- 2026-03-20: For this map surface, richer geography should come from layered land/border rendering before jumping to a higher-resolution atlas; `world-atlas` 50m materially increased the route chunk size, while 110m plus a country-border mesh delivered most of the visual gain at a much lower payload cost.
- 2026-03-20: For brand renames in this repo, update human-facing copy, metadata, auth statements, and test fixtures together, but avoid renaming internal package scopes or deployment slugs unless there is explicit migration time for the whole workspace.
- 2026-03-20: If map geography drives battle semantics, encode the side split in the source country dataset and reuse it in demo/battle composition; deriving it separately in the map and fight layers leads to label/color drift as soon as the player can spawn on either side.
- 2026-03-20: For this frontend, page-level background washes read like broken side gradients once the map is framed; keep atmosphere inside `.world-map::before` and let the outer shell stay almost flat white.
- 2026-03-20: If the map itself is meant to feel archival and precise, keep the cartography monochrome; red/blue accents land better in side ownership, health, badges, and battle chrome than on country strokes and route lines.
- 2026-03-20: For broad theme reversals in `apps/web/src/styles/globals.css`, patch the runtime in smaller selector blocks after reading the current file; that stylesheet drifts too quickly for one monolithic diff to stay reliable.
- 2026-03-20: Result overlays in this app need click handling isolated from the map beneath them; stop propagation on the result panel itself or the dismissal click can bleed through into a fresh country selection.
- 2026-03-20: On small screens, a display heading can still overflow even after `white-space: nowrap`; verify branded title lockups with a real mobile screenshot and tune both font size and letter spacing together.
- 2026-03-20: For oversized display fonts on centered landing screens, width constraints on the parent can force unwanted multi-line composition on desktop too; validate the parent max width before treating the heading as the only problem.
- 2026-03-20: If mobile browser rails or safe-area bands look off-theme, check the document root and viewport metadata first; `body` styling alone is not enough for Safari/standalone chrome surfaces.
- 2026-03-20: On interactive SVG maps, pointer capture can make drag feel robust but silently break child path taps; prefer bounded local pointer tracking unless you truly need capture outside the surface.
- 2026-03-20: If cooldown rules depend on wallet arsenal size, do not read the timer duration from dormant store state; sync the loaded guns into the store or compute from the active arsenal source, otherwise demo/local cooldowns silently fall back to the wrong duration.
- 2026-03-20: On layered game screens, keep map-only affordances and idle HUD surfaces scoped to the idle phase; leaving them mounted beneath battle/result overlays creates visual clutter and can even intercept clicks in the result state.
- 2026-03-20: If a framed map starts reading like it has vertical rails, check for a horizontal `90deg` wash inside the container overlay before adjusting borders or shadows; center-weighted atmosphere holds depth without dirtying the edges.
- 2026-03-21: For demo battle playback, remove round/progress telemetry if it reads like a timer; decorative simulation scaffolding can make the combat surface busier without adding meaningful information to the review flow.
- 2026-03-21: Embedded battle chat should not reuse the full chat panel contract; strip it down to a compact battle-log variant or it competes with the primary combat hierarchy on mobile and small laptops.
- 2026-03-21: On this map surface, keep the country callout box stroke and the leader-line stroke on separate selectors; coupling them under the side modifier makes it impossible to get a white framed label with an accent connector.
- 2026-03-21: Any interactive overlay mounted under `.site-shell__content` needs its own `pointer-events: auto`; otherwise the fixed world map can intercept clicks from visible buttons even when the overlay has the higher visual z-order.
- 2026-03-21: For branded glitch treatments, pushing RGB channel separation too far can read as color styling instead of distortion; if the ask is “just distortion,” keep the effect monochrome and sell it through slice jumps, skew, blur, and side tearing.
- 2026-03-21: In `apps/web/src/styles/globals.css`, large multi-section polish patches are brittle; reread the exact selector blocks and patch the file in smaller region-specific chunks instead of attempting one monolithic diff.
- 2026-03-21: For the VS reveal, keep the live center stamp inside the dedicated duel-grid center slot and check feature-local CSS before trusting global `vs-screen` selectors; stale parallel layout systems can make the headline look centered to the viewport instead of centered between fighters.
- 2026-03-21: When a provided database URL contains `&` query parameters, do not `source` the `.env` file directly in the shell; pass the values as quoted inline environment variables or use a parser-safe loader, otherwise zsh will split the connection string and fail before the migration even starts.
- 2026-03-21: Root-level Vercel functions in this monorepo do not reliably resolve nested workspace package imports during the platform TypeScript pass; for small shims like `api/[[...route]]`, use a plain `.js` entry and explicit paths into the workspace package when the first deploy shows module-resolution failures.
- 2026-03-21: In this Vercel setup, a blanket SPA rewrite to `/index.html` will happily swallow `/api/*` and return HTML for health checks; exclude `/api/` explicitly from the catch-all rewrite before trusting a deployed function path.
- 2026-03-21: For this monorepo on Vercel, the root serverless shim should import the built API artifact, not raw workspace source; runtime bundles cannot rely on `../apps/api/src/...` existing after deployment, so the Vercel build must explicitly produce `apps/api/dist` first.
- 2026-03-21: Even after switching the Vercel serverless shim to the built API artifact, a root `api/*.js` function may still execute as CommonJS under Node; when loading an ESM workspace bundle like `apps/api/dist/index.js`, use a cached dynamic `import()` inside the handler instead of a top-level static import to avoid `ERR_REQUIRE_ESM`.
- 2026-03-21: Before debugging Vercel monorepo build filters, inspect `.vercelignore`; excluding `apps/api/**` made every later API packaging symptom misleading because the package was being dropped before build and runtime ever saw it.
- 2026-03-21: If a serverless platform runs Node but the workspace package builds with `bun build --target bun`, treat that as a production blocker immediately; the bundle may build locally and still crash remotely, so shared deploy artifacts must target the actual runtime, not the preferred local dev runtime.
- 2026-03-21: On Vercel Node functions, do not wire Hono by manually calling `.fetch()` from the route file; use the proper `hono/vercel` adapter so the handler receives the platform's `req/res` shape correctly and health endpoints do not hang behind a mismatched runtime boundary.
- 2026-03-21: Correction: in this monorepo's Vercel packaging path, `hono/vercel` can fail at runtime because the adapter file is not present in the root function bundle even when `hono` is installed. The reliable fix here is a thin Node `req/res` to Fetch `Request` bridge in `api/[[...route]].js`, backed by a Node-compatible CommonJS API build.
- 2026-03-21: If a serverless CommonJS bundle consumes a workspace package that only exports ESM, fix the package boundary explicitly instead of hoping the bundler inlines it. `@warpath/shared` needed real `require` exports and CommonJS artifacts before the Vercel API runtime could load `apps/api/dist/index.cjs` safely.
- 2026-03-21: When Drizzle generates a migration that adds a new required column to an existing table, inspect the SQL before applying it live; `ALTER TABLE ... ADD COLUMN ... NOT NULL` needs a backfill step first or production rows will block the migration.
- 2026-03-21: Once API middleware starts depending on the database, route tests that import the real app must stub that middleware explicitly or they will try to hit the default local Postgres during auth-only coverage and fail before the route logic runs.
- 2026-03-21: For wallet UX in this app, do not auto-connect whatever connector happens to be first; desktop browsers can expose multiple injected providers and mobile environments vary between wallet browsers and WalletConnect deep links, so a visible connector chooser is the more reliable production path.
- 2026-03-21: Correction: if the required UX is still one-click connect, keep the decision engine platform-aware instead of QR-first. On desktop, prefer injected extension transport before SDK/deeplink connectors so MetaMask and other browser wallets open natively; on mobile, prefer wallet-app connectors before generic injected fallback.
- 2026-03-21: If you want one-click wallet connect without ugly choices, do not lead desktop through MetaMask/Coinbase SDK connectors. Register targeted `injected({ target: ... })` connectors for real browser extensions first, then keep SDK connectors only as fallback transport for mobile app handoff.
- 2026-03-21: For mobile Safari in this app, do not deep-link users into the MetaMask in-app browser when the requirement is “approve in wallet, then return to Safari.” Use the WalletConnect connector with the provided project ID, feature the desired wallets via official explorer IDs, and leave the app browser path out entirely.
- 2026-03-21: Do not claim a frontend change is live from deploy status alone. After any Vercel production deploy that changes CSS or assets, fetch the public domain HTML, capture the emitted asset hash, and inspect the served bundle contents for the exact new values before reporting success.
- 2026-03-21: Keep branded/product headings and combat-state headings on separate selectors. A shared display-font group made ordinary surfaces like the shell logo and leaderboard masthead inherit the glitch font when only battle-impact words were supposed to keep it.
- 2026-03-21: On the world map, selected-player markers and active/opponent markers should not share the same state selectors; the chosen area wants a static solid dot, while the live opponent marker still needs the animated halo.
- 2026-03-21: When a UI note says “change the solid dot only,” do not remove the surrounding animation layer. For this map, the correct interpretation is red inner marker with the existing blue pulse preserved.
- 2026-03-21: For local custom fonts in the web app, do not point CSS at an arbitrary repo-root file. Copy the font into `apps/web/public`, register it with `@font-face`, and scope it with a dedicated variable so Vite and Vercel ship the same asset.
- 2026-03-21: When the user references a screenshot for a header/logo treatment, confirm you are styling the live fixed header element, not the separate connect-screen hero lockup. In this app those are different surfaces with different selectors.
- 2026-03-21: Do not blindly reuse the connect-screen glitch animation on the fixed header logo. The map-page header mark needs a calmer static distressed treatment; matching the screenshot means matching the surface behavior, not just the font family.
- 2026-03-21: If the exact header artwork exists in the repo, stop approximating it in CSS. Ship the provided image asset through `public/` and mount that in the fixed header instead of simulating it with typography.
- 2026-03-21: When a fixed header uses an image wordmark, size the asset by height and make the page offset track the explicit rail height. Width-only logo scaling let the `header.jpg` art quietly exceed the reserved header footprint and overlap the leaderboard masthead.
- 2026-03-21: Before assuming an NFT contract can enumerate owner token IDs, probe `supportsInterface(0x780e9d63)` or test `tokenOfOwnerByIndex` directly. This Glock Node contract is non-enumerable, so `balanceOf` works but owner indexing reverts and yields an empty arsenal unless you use an ownership indexer such as Alchemy.
- 2026-03-21: For non-enumerable NFT collections, do not stop at a single third-party indexer fallback. Use a layered ownership strategy: on-chain `balanceOf` for count, fast indexed ownership when available, and on-chain `Transfer` log reconstruction as the fail-safe path.
- 2026-03-21: On Vercel, do not assume the optional catch-all `api/[[...route]].js` will behave like a full nested API catch-all in this workspace. The nested runtime paths were only reliable after switching the serverless entrypoint to `api/[...route].js`.
- 2026-03-21: Even with a root catch-all serverless route, this Vercel setup can still fail to resolve nested dynamic API paths reliably. For production-critical endpoints like `/api/guns/:address` and `/api/battles/...`, add explicit nested function files that delegate to the shared handler.
- 2026-03-21: Do not run overlapping builds that both clean `packages/shared/dist`; `tsup --clean` on the shared package can race another build and fail with `ENOENT` while unlinking generated declaration files.
- 2026-03-21: For custom HTML metadata replacement in Vite, do not use `%VITE_*%` placeholders unless you want Vite's built-in env expansion. If you need a fallback-aware transform, use a neutral token like `__SITE_URL__` and replace it in `transformIndexHtml`.
- 2026-03-21: In this monorepo, TypeScript package-boundary resolution to `@warpath/shared` is less reliable than source-path resolution during local typecheck/build. For app/API typechecking, prefer explicit `paths` mappings to `packages/shared/src` over brittle declaration-only dependency assumptions.
- 2026-03-21: In this frontend, “polish” animations drift into layout work quickly if stat/health bars animate `width`. Keep recurring combat motion transform-led with `scaleX` and explicit `transform-origin` so the UI stays cinematic without paying layout cost every tick.
- 2026-03-21: When removing a demo/runtime bypass from this app, cut the branch through providers, connect state, guns, leaderboard, matchmaking, battle chrome, copy, and env docs in one pass. Leaving even one `DEMO_MODE` branch behind makes the live product semantics lie about whether a battle came from real queued wallets.
- 2026-03-22: API route tests in this monorepo can execute against a stale built `@warpath/shared` package even when source is current. Make the API test script build `@warpath/shared` first, or route the runtime to shared source explicitly, before trusting auth/message-builder coverage.
- 2026-03-22: When probing live endpoints from zsh, always quote URLs containing `?` query strings. Unquoted verification commands can fail at the shell level and create a false negative that looks like an API problem.
- 2026-03-22: If you remove repeated wallet signatures from a user-generated channel, replace them with a short-lived wallet-authenticated session token, not an unauthenticated address field. The correct UX improvement is fewer signatures, not weaker identity.
- 2026-03-22: For first-party avatar upload without a dedicated object store, client-side square crop/compression to a bounded `data:image/...` payload keeps the feature shippable and predictable. The server validator must explicitly allow safe image data URLs instead of only remote `https://` URLs.
- 2026-03-22: For WalletConnect on a multi-domain Vercel app, do not hard-bind the connector metadata URL to an env value that can lag behind the real public host. Use the runtime `window.location.origin` in the browser so mobile app-switch return paths and wallet metadata stay aligned with the page the user actually opened.
- 2026-03-22: Mobile WalletConnect flows should use one deterministic connector attempt per click. Looping across multiple connectors after the first handoff can invalidate the pending session and leave MetaMask open with nothing to approve.
- 2026-03-22: If product explicitly chooses “connected wallet unlocks chat immediately” over separate chat auth, remove the second gate end-to-end instead of trying to disguise it with prewarming. Partial bootstrap still feels like a second prompt because the user experiences another auth boundary.
- 2026-03-22: When verifying a routed frontend behavior on a Vite production deploy, do not stop at the main `index-*.js` bundle. Route-level code can live in lazy chunks like `ChatPage-*.js`, so the live proof must inspect the actual route chunk that contains the behavior in question.
- 2026-03-22: Treat ENS as resolved identity fallback, not persisted profile state. If a wallet has no custom display name, return `ensName` alongside `displayName` and let the UI resolve `displayName -> ensName -> shortened address` so users do not accidentally save or overwrite names they never chose.
- 2026-03-22: In this app, the `players` table is broader than the leaderboard population because wallets are inserted before first combat. Any leaderboard query must explicitly filter for real combat history (`wins > 0 OR losses > 0`) instead of assuming every player row belongs on the standings board.
- 2026-03-22: For live chat timestamps, relative time is more readable than a bare clock string, but keep the absolute local timestamp available in `title` so the UI gains readability without losing exactness.
- 2026-03-22: When a product rename is presentational rather than structural, change visible labels and accessibility copy first, but keep stable routes like `/chat` unless the user explicitly asks to migrate URLs and backlinks too.
- 2026-03-22: Any deterministic RNG helper that feeds `encodePacked(['bytes32', ...])` must normalize arbitrary string seeds to a real 32-byte hex first; tests often exercise human-readable seeds even when production uses hashes already.
- 2026-03-22: When a battle payload becomes a discriminated lifecycle union (`committed`/`resolving`/`resolved`/`failed`), update the frontend polling semantics and test fixtures in the same pass. Leaving `matched => resolved` assumptions behind will compile in some paths and still break the live combat flow.
- 2026-03-22: Privacy toggles are not real privacy if only the UI respects them. Any public API that can enumerate the same resource set must enforce the visibility rule server-side on direct reads as well as feed/list responses.
- 2026-03-22: Treat database-backed cache refreshes as writes for security review purposes. A public `GET` endpoint is still mutating state if it persists lookup snapshots, so keep those writes off unauthenticated read paths.
- 2026-03-22: For a battle reveal with asymmetric fighter cards, centering the `VS`/`FIGHT` copy requires symmetric grid columns, not just `justify-self: center` on the text node. If the lane is visually biased, fix the duel grid first.
- 2026-03-22: In the VS reveal, symmetric columns alone are not enough if both fighter shells default to start alignment. Anchor the left shell to the inside-right edge and the right shell to the inside-left edge, or the center stamp will still drift toward the right card even with a centered middle track.
- 2026-03-22: When product asks for a temporary gameplay-rule shutdown, disable it at the narrowest backend contract boundary that fans out to all consumers. Here, `players.ts` was the right seam because matchmaking checks, gun payload cooldown reads, and post-battle cooldown writes all flowed through that one service.
- 2026-03-23: When battle odds are tuned in `@warpath/shared`, search the web-side fallback helpers and UI copy for duplicated percentages before calling the rebalance done. This repo still had a dormant `apps/web/src/lib/stats.ts` hardcode and a visible `+10%` selector label after the engine moved to shared constants.
- 2026-03-23: For one-off product-provided media files in this Vite app, the fastest reliable production path is `apps/web/public/assets` plus a direct `/assets/...` reference. That avoids inventing a new source-asset pipeline when the file just needs to ship and be addressable at runtime.
- 2026-03-23: For phase-scoped audio in this battle UI, mount the sound at the phase component boundary instead of the parent overlay. That keeps start/stop behavior coupled to the exact screen lifecycle and avoids manual phase bookkeeping in `GameOverlay`.
- 2026-03-23: Reuse the same phase-boundary pattern consistently across matching and fighting audio. Mounting each loop inside its own screen component keeps overlap risk low and makes teardown happen automatically when the UI phase advances.
- 2026-03-23: When adding a delayed phase audio start, keep the timeout id in a ref and clear it during unmount. Otherwise a fast phase transition can trigger playback after the UI has already moved on.
- 2026-03-23: For one-shot phase cues like the `FIGHT` stamp, bind playback to the state transition that flips the UI rather than duplicating the reveal delay in separate timers. That keeps the sound aligned even if the reveal timing is tuned later.
- 2026-03-23: When product swaps an audio source but the phase timing stays the same, replace the asset path and keep the existing lifecycle logic intact. Do not rebuild working delay/cleanup behavior just because the sound file changed.
- 2026-03-23: Result sounds belong on the result overlays themselves, not in the parent battle flow. Mounting win/loss audio at the overlay boundary keeps outcome cues aligned with the visible state and prevents them from firing on the wrong branch.
- 2026-03-23: When product explicitly wants one-shot cues, set `audio.loop = false` in code even though the browser default is non-looping. Making it explicit avoids regressions when sounds are copied from loop-based patterns like the original matchmaking audio.
- 2026-03-23: Tiny, battle-critical overlays are not good lazy-split targets on a frequently redeployed site. If a result screen must always open cleanly, keep it in the parent battle chunk instead of forcing a second hashed chunk fetch at the phase boundary.
- 2026-03-23: On a Vite SPA that is redeployed frequently, add a one-shot stale-chunk recovery path for dynamic imports. Existing tabs can legitimately request old hashed assets after a deploy, and without a guarded reload the app will surface a hard runtime error instead of self-healing.
- 2026-03-23: For favicon swaps in this app, prefer a stable file in `apps/web/public/` referenced directly from `index.html`. That keeps browser-tab icon updates decoupled from hashed JS/CSS bundles and avoids unnecessary favicon-specific asset plumbing.
- 2026-03-22: When NFT metadata is the product truth, do not let battle/replay UI fall back to a local registry name map. Put the metadata-backed name on the API payload and make all combat surfaces read from that canonical field.
- 2026-03-22: For this Glock Node collection, the product truth for gun titles is the curated 101-item tokenId map, not the NFT metadata `name` field. Use metadata for media/traits, but resolve names from the strict collection list so casing and wording stay exact.
- 2026-03-22: Do not treat a successful Vercel deploy as a complete backend rollout when the API depends on Postgres schema changes. Before calling matchmaking or battle changes live, verify the production database actually has the new columns the deployed code inserts into, or `/api/battles/queue` will fail with a runtime 500 despite healthy builds and aliases.
- 2026-03-22: If product says “stats are just for show,” remove every stat-derived effect, not only the obvious winner formula. Attack order, dodge chance, crit edge, damage scaling, tie-breaks, and even replay pacing can all smuggle stat weight back in unless they are audited together.
- 2026-03-22: On this world map, the topo centroid is still the correct default marker anchor. If one country needs a manual dot correction, scope the override to that country instead of switching the whole dataset onto hand-authored `markerX` / `markerY` coordinates.
- 2026-03-22: France is a bad centroid candidate in this atlas because the country geometry includes non-mainland polygons. For France, anchor the dot from projected mainland lat/long, not from the feature centroid and not from an arbitrary percent-of-viewBox guess.
